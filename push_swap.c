#include "push_swap.h"

int	main(int argc, char **argv)
{
	t_state	*state;

	if (argc < 2)
		return (0);
	else

		if (parse_input(argc, argv, state))
		{
			// Handle error in parsing input
		}

	}
}
